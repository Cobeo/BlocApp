<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="BlocApp.Views.BlocListPage">
    
    <ListView x:Name="BlocListView">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Orientation="Horizontal">
                        <Label Text="{Binding Path=ID, StringFormat='ID: {0} ' }" TextColor="White"/>
                        <Label Text="{Binding Path=Location, StringFormat='Location: {0} ' }" TextColor="White"/>
                        <Label Text="{Binding Path=Vermin, StringFormat='Cotation: {0} ' }" TextColor="White"/>
                        <Label Text="{Binding NumberDone, StringFormat='Number Done: {0}' }" TextColor="White"/>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>